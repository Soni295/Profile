/*
const tecno = document.getElementById('technologies')
const technologies = [
  ['JavaScript', 'TypeScript', 'Python', 'Bash'],
  ['React', 'Redux', 'Node', 'Babel', 'Express'],
  ['Html', 'Css', 'Sass', 'Style Component', 'Bootstrap'],
  ['Mysql', 'Sqlite', 'Git', 'Npm']
]

technologies.forEach( arr => {
  const arrOfP = value => create('p', {innerHTML: value})
  const first = create('div', {}, arr.map(arrOfP))
  const second = create('div', {}, arr.map(arrOfP))
  const row = create('div', {}, [first, second])
  tecno.appendChild(row)
})
*/
